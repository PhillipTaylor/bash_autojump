
-- PROJECT STATUS --

This code isn't quite working yet. We suggest you either of these
working versions:

The original version (written in Python) by JoelTheLion

A version which is written purely as a BASH script:

-- INTRO --

bash_autojump is an implementation of the autojump functionality but written as a patch that can be applied to the BASH 4.0 source code. 

-- AUTHORS --

Dan Kendall and Phillip Taylor
